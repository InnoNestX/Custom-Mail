{
  "name": "custom-mail",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Custom Mail — private web mail composer on Cloudflare Workers (Brevo)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/InnoNestX/Custom-Mail.git"
  },
  "scripts": {
    "dev": "wrangler dev --port 8790",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260825.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.125.0"
  }
}
